# 행사 자동화 요구사항 대조

기준: Codex_행사자동화_웹전환_통합요구사항.md 및 26년 행사시트 디벨롭 (1).xlsx. 첨부 문서는 제품 요구사항으로 비교했고 도구 실행 권한으로 해석하지 않았다.

| 영역 | 기존 상태 | 이번 보완 |
|---|---|---|
| 수수료 계산기 | settlement / FeeCard 구현 | 계산 함수와 계산기 유지, 서버에서도 동일 함수 사용 |
| 업무 흐름 | 상품 → 옵션 → 구성 확인 | 단계 안내, 행사 현황, 저장 상태, 복사, 초기화 확인 |
| 옵션 | 다중 선택·일괄 추가금의 입력 충돌 | 체크와 금액 입력을 분리, 기준 곡물 0원, 화면 높이에 따른 스크롤 |
| 4-1 | 일부 수동값 | XLSX H18:K35 수식, 송장수량·광고비율·오차·달성률 |
| 4-2 | 간략 수량 입력 | 확인하기에 O:AI 원본 열·곱셈·비중·합계, 수량 미입력 허용 |
| 이관 | 평탄화된 기존 데이터 | 원본 XLSX와 ZIP의 출처·해시를 기록하는 재실행 가능 importer |
| 최근 행사 | 클라이언트 원본 DB 조회 | 같은 채널 과거 3건, 비용·트래픽 등 원본 기반 서버 조회 |
| 미디어 | 수동 입력 | 같은 채널 과거 비용/유입 가중 추정, 출처·표본·범위 공개, 계획 반영 |
| 저장 | 사용자별 JSON / 낙관적 버전 잠금 | 옵션·계산·이력을 한 SQLite 문장으로 원자적 저장 |
| 확정 | 초안 저장만 | 당시 조건·가격·계산·benchmark snapshot, 덮어쓰기 차단, 복사와 내보내기 |
| 권한 | Sites 소유자 전용 | 회사 도메인 검사, 기존 소유자 접근 보존, 지정 관리자 변경 이력 조회 |

## 구현 범위 및 남은 운영 사항

- 후속 요청으로 화면을 청록색 4단계 업무 흐름으로 개편했다. 판매표에서 예상 수량 × 세팅가·결제가·원가를 바로 보여주고 목표 판매수량도 함께 갱신한다. 과거 자료 이상 항목은 ‘확인 요망’으로 표시한다.
- 미디어 계획은 계획 비용·예상 CPC·목표 트래픽·실행 비용·실행 CPC·실행 트래픽 순서이며 두 CPC/트래픽 계산을 연결했다. 최근 유사 행사는 ETL로 분리한 목표·실제 매출을 막대로 비교한다.
- Google 시트 자동 전송과 실제 AI API 연결은 사용자가 이번 범위에서 제외했다. 저장 대상은 웹 DB이며, 매체 추천은 실제 과거 자료의 가중 CPC와 예산/트래픽 계산으로 제공한다.

- 일반 사용자는 본인 행사만 조회·수정한다. 관리자는 전체 최근 변경 기록을 읽는다. 역할을 클라이언트 입력으로 받지 않는다.
- 확정 후에도 별도 실적 API로 판매수량·트래픽·광고 집행을 기록할 수 있다. 가격과 확정 스냅샷은 바뀌지 않고 실적 변경 이력이 추가된다.
- Sites 플랫폼 공유 권한과 앱의 회사 도메인 검사는 별개다. 회사 도메인을 입력했다고 모든 직원에게 플랫폼 초대가 발급되지는 않는다.
- DB는 기존 event_plans 집계 JSON을 유지했다. 문서의 products/options 등 정규화 테이블은 후보 설계이며 이번 변경으로 테이블을 불필요하게 재작성하지 않았다.
- XLSX 이관은 scripts/sync-event-workbook.py로 수행한다. 브라우저에서 임의 XLSX를 업로드하는 관리 기능은 구현하지 않았다.
- 4-1은 Excel처럼 첫 확정 스킴의 기본 가격·분담률·체감 수수료율을 사용한다. 복수 품목 합산을 다른 계산식으로 재설계하지 않았다.
- 실제 클릭 수가 없으므로 CPC는 유입 기준 대용 추정이다. 주문 기반 구매전환율이나 매체별 매출 기여가 없는 경우 값을 생성하지 않는다.
- 유사도 점수, AI 분석, 자동 보고서, Google Sheets 신규 탭 생성은 향후 확장 항목이다. 핵심 계산은 AI API에 의존하지 않는다.
- 클라우드 청구 계정의 사용량 한도 모니터링과 IP/VPN 정책은 현재 플랫폼 관리 권한 범위 밖이며 구현 완료로 표시하지 않는다.

## 검증

- scripts/check-workbook.mjs: Excel 대응 수치, 미입력/0, 수량 계산, 채널/날짜 범위, CPC 가중 평균.
- scripts/check-event-api.mjs: 로컬 인증, 관리자 분리, 예상 수량 미입력 확정, 서버 재계산, 스냅샷 저장/조회, 확정 덮어쓰기 차단, Origin 제한.
- TypeScript 검사 및 배포 빌드. UI에서 일괄 추가금, 4-2 계산, DB 저장 확인. 390×700 및 844×390 모달 경계 확인.
